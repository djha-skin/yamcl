I am not satisfied with the tests that have been written for the scalars. I wrote a set of tests myself. S

I want you to look through the YAML 1.2 spec. Its address is mentioned in the
README and other places like AGENTS.md and maybe CONTRIBUTING.md .

For every example piece of data, I want you to create a test case for scalars
in the `tests/scalars.lisp` file. I want a subpackage
`com.djhaskin.yamcl/tests-scalars` to be made in that file and an addition for
it to be put in the ASD file so its contents are run when `asdf:test-system` is
run as we have previously discussed. I want the scalar tests moved out of the
`tests/main.lisp` file and into the `tests/scalars.lisp`. In fact, that file
was really poorly written. The tests I want you to write should look like the
tests writtn for the `rssm` project, found under `../rssm`. Check the `tests/`
files in that project and the `*.asd` file in that repo for good examples of
how to set up good tests. In particular, I want parachute used, not something
else. Parachute examples can be found here:
https://codeberg.org/shinmera/parachute/raw/branch/master/example.lisp and docs
here: https://shinmera.com/docs/parachute/

Transcribe any relevant tests you have already written into this new format and
in their own file, `tests/scalars.lisp`, as mentioned previously. If you do
your job right, there should be no weird `defmacro` or other such helpers. You
do not need them. Check the examples above if you don't know how to write a
test without some bespoke macro.

In this file, I also, as I have stated, want each scalar listed in any of the
example listings in the YAML Spec to get its own test.

For example, in the spec, consider the example labeled "Example 2.1 Sequence of
Scalars (ball players)". Its listing is as follows:

```
- Mark McGwire
- Sammy Sosa
- Ken Griffey
```

For this listing, I want a test written like so:

```
(deftest example-2.1-sequence-of-scalars-ball-players
   (is string= (yamcl:parse-from-string "Mark McGwire") "\"Mark McGwire\"")
   (is string= (yamcl:parse-from-string "Sammy Sosa") "\"Sammy Sosa\"")
   (is string= (yamcl:parse-from-string "Ken Griffey") "\"Ken Griffey\""))
```

The above listing is of course assuming you have pulled in the current parser
in the main package under the package-local nickname `yamcl`.

In this effort, I have decided that we need to include multiline strings, so do
cover those cases as well, as in the listing "Example 2.13 in literals,
newlines are preserved".

I can find 136 instances in that document of the characters `Example `, the
word "example" followed by a space, which presumably means there are that many
example listings. Please don't miss a single one.

Also, please don't repeat scalar tests. No need to repeat the assertion `(is
string= (yamcl:parse-from-string "Mark McGwire") "\"Mark McGwire\"")`. That
gets used a lot. Try to include important, useful, different examples, but
favor getting more examples over fewer.


Follow the conventions set forth in the the library `ieee-floats` for weird
esoteric IEEE float values: https://ieee-floats.common-lisp.dev/ . For example,
for positive infinity, the parser should yield simply `:positive-infinity` and
conversely `:negative-infinity` for negative infinity, together with the
obvious `:not-a-number` for NaN. You may need to change the current logic for
this.

Once you are satisfied that all such examples are captured in code, make a git
commit. Then fix the parser to work with the tests.
